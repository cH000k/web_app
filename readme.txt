To create venv for windows using VS Code
py -m venv venv

To activate venve for windows (you must be in the same folder directory)
venv\Scripts\activate

To launch python as an app
python app.py

To save copy of all python package version
pip freeze > requirements.txt


Initial Git in VS Code
Use 64-bit installer (not portable ver) 
https://git-scm.com/download/win